{
  "name": "omp-worktree-guard",
  "version": "0.2.0",
  "description": "omp plugin: enforce the git-worktree isolation policy — agent file edits never land in the main checkout",
  "license": "MIT",
  "keywords": [
    "omp",
    "oh-my-pi",
    "pi-coding-agent",
    "plugin",
    "worktree",
    "guard",
    "hook"
  ],
  "repository": {
    "type": "git",
    "url": "github:Daviey/omp-worktree-guard"
  },
  "files": [
    "hooks/",
    "skills/",
    "extensions/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "test": "bun test hooks/pre/*.test.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0"
  },
  "omp": {
    "name": "worktree-guard",
    "description": "Block edit/write on the main checkout of any git repo; allow linked worktrees and .worktrees/ sandboxes. Fails open.",
    "extensions": ["extensions/wt.ts"],
    "features": {
      "wt-command": {
        "description": "/wt slash command: create/merge/list/clean linked worktrees",
        "default": true
      },
      "worktree-skill": {
        "description": "Auto-loaded skill teaching agents the sanctioned worktree workflow",
        "default": true
      }
    }
  }
}
